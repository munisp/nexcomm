# Supply these as protected GitHub environment variables/secrets or source them in
# an authorized operator shell before running tests/integration/secured_staging_e2e.py.
# Do not point these values at production.
STAGING_GATEWAY_URL=https://gateway.staging.example.invalid
STAGING_KEYCLOAK_URL=https://auth.staging.example.invalid
STAGING_PERMIFY_URL=https://permify.staging.example.invalid
STAGING_TEMPORAL_HEALTH_URL=https://temporal.staging.example.invalid
STAGING_DAPR_HEALTH_URL=https://dapr-gateway.staging.example.invalid
KEYCLOAK_REALM=nexcom
STAGING_E2E_TIMEOUT_SECONDS=15
STAGING_E2E_RESULT_PATH=test-results/secured_staging_result.json

# Use a dedicated, low-privilege account; keep values in GitHub Environment secrets.
STAGING_TEST_USERNAME=
STAGING_TEST_PASSWORD=
