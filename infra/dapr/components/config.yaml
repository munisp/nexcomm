apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: nexcom-runtime
spec:
  tracing:
    samplingRate: "1"
  metric:
    enabled: true
  features:
    - name: Resiliency
      enabled: true
